import React from 'react';

const Settings = () => {
  return (
    <div>
      <h2>Settings</h2>
      {/* Add user settings form here (e.g., change password, update profile) */}
    </div>
  );
};

export default Settings;
